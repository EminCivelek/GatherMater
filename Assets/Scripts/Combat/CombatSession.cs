// Static data carrier — set before loading CombatScene, read by CombatSelectionUI / CombatManager
public static class CombatSession
{
    public static MobConfig SelectedMob;
    public static int PullSize = 1;
}
